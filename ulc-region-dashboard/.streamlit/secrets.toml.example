# Copy this file to `secrets.toml` in the same `.streamlit/` folder.
# `secrets.toml` is gitignored — it will not be pushed to GitHub.
#
# Streamlit Community Cloud: paste the same keys in the app’s Secrets UI (TOML).

DASHBOARD_PASSWORD = "replace-with-a-strong-password-share-only-with-teammates"
# DATABRICKS_TOKEN = "dapi_..."  # optional here if you use ../databricks-dbx/.env instead
